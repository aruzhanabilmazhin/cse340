<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %> | CSE Motors</title>

  <!-- Подключаем CSS из public/css -->
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <!-- Header -->
  <header>
    <h1><a href="/" class="site-title">CSE Motors</a></h1>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/cars">Cars</a></li>
        <li><a href="/about">About</a></li>
        <li><a href="/contact">Contact</a></li>

        <% if (account) { %>
          <!-- Пользователь вошёл -->
          <li><a href="/account/manage">My Account (<%= account.firstname %>)</a></li>
          <li><a href="/account/logout">Logout</a></li>
        <% } else { %>
          <!-- Пользователь не вошёл -->
          <li><a href="/account/login">Login</a></li>
          <li><a href="/account/register">Register</a></li>
        <% } %>
      </ul>
    </nav>

    <% if (messages && messages.length) { %>
      <div class="flash-messages">
        <% messages.forEach((msg) => { %>
          <p><%= msg %></p>
        <% }) %>
      </div>
    <% } %>
  </header>

  <!-- Main content -->
  <main>
    <%- body %>
  </main>

  <!-- Footer -->
  <footer>
    <p>&copy; <%= new Date().getFullYear() %> CSE Motors. All rights reserved.</p>
  </footer>
</body>
</html>
